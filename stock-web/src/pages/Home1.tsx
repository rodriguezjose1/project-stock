
import {Box, Typography} from "@mui/material";
import { FC, Fragment, ReactElement } from "react";

export const HomeOne: FC<any> = (): ReactElement => {
    return (
        <div>
            <Typography variant="h3">HomeOne</Typography>
        </div>
    );
};