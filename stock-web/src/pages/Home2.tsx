import { Typography } from "@mui/material";
import { FC, ReactElement } from "react";

export const HomeTwo: FC<any> = (): ReactElement => {
  return (
    <div>
      <Typography variant="h3">HomeTwo</Typography>
    </div>
  );
};
